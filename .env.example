# Environment variables for llm-perf-testing
# Copy this file to ".env" and fill in your values.
# NOTE: The app does not automatically load .env.
#  - Option A: Set variables in your shell (recommended).
#      Windows (PowerShell):
#        $env:OPENAI_API_KEY = "sk-..."
#      macOS/Linux (bash/zsh):
#        export OPENAI_API_KEY="sk-..."
#  - Option B: Add `from dotenv import load_dotenv; load_dotenv()` to app.py to auto-load .env.

# OpenAI API Key used by the app and evaluation tools
OPENAI_API_KEY=your_openai_api_key_here
